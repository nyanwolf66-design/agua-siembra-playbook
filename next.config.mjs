/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable static export for pure static deploy (optional — remove if using Vercel with SSR)
  // output: 'export',
}

export default nextConfig
